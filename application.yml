spring:
  datasource:
    url: jdbc:mysql://localhost:3307/User_System?useSSL=false&allowPublicKeyRetrieval=true
    username: user
    password: userpassword
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
